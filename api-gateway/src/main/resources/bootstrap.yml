# server name
spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
        # env
      profile: test
        # branch
      label: master

# eureka path
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
