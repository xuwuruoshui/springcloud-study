# eureka path
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#server name
spring:
  application:
    name: order-service
  # Specify which registry to read from
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      # env
      profile: test
      # branch
      label: master
